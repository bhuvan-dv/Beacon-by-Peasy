# Next.js 14 declares React 18 as its peer dependency, but this project pins
# React 19 per the target stack. legacy-peer-deps lets `npm install` resolve
# cleanly instead of failing with ERESOLVE.
legacy-peer-deps=true
